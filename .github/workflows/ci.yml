name: Pull Request

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: ["main"]

jobs:
  run-tests:
    uses: ./.github/workflows/pytest.yml
  linter:
    uses: ./.github/workflows/linter.yml
